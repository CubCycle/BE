package com.example.cupcycle.repository;

import com.example.cupcycle.entity.ReturnEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReturnEventRepository extends JpaRepository<ReturnEvent, Integer> {
}
